# Uncomment the next line to define a global platform for your project

platform :ios, '12.0'
use_frameworks!

target 'ios-github-connector' do
  pod 'SwiftyBeaver'
  pod 'AlamofireObjectMapper', '~> 5.1'
  pod 'Alamofire', '~> 4.7'
end
